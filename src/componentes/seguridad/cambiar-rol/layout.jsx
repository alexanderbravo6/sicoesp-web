import React from 'react'

export const metadata = {
    title: "Cambiar Rol",
    description: "Módulo para cambiar roles",

};
const RolesLayout = ({ children }) => {
    return (
        <>
            {children}
        </>
    )
}

export default RolesLayout